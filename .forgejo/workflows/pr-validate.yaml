name: "Pull Request: Validate"

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.head_ref }}-pr-validate
  cancel-in-progress: true

jobs:
  pr-info:
    name: Collect PR metadata
    runs-on: codeberg-tiny-lazy
    outputs:
      isRenovatePR: ${{ startsWith(github.head_ref, 'renovate/') && 'true' || 'false' }}
      isFork: ${{ github.event.pull_request.head.repo.full_name != github.repository && 'true' || 'false' }}
    steps:
      - name: Save PR data to file
        env:
          PR_NUMBER: ${{ github.event.number }}
        run: |
          echo $PR_NUMBER > pr_number.txt

      - name: Store pr data in artifact
        uses: https://code.forgejo.org/forgejo/upload-artifact@v4
        with:
          name: pr_metadata
          path: ./pr_number.txt
          retention-days: 5

  pr-changes:
    name: Collect PR changes
    runs-on: codeberg-tiny-lazy
    outputs:
      changed-files: ${{ steps.changed-files.outputs.changed_files }}
    steps:
      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get Changed Files
        id: changed-files
        uses: https://github.com/bjw-s-labs/action-changed-files@a9a36fb08ce06db9b02fbd8026cc2c0945eb9841 # v0.6.0
        with:
          patterns: chart/**

  pre-commit:
    uses: ./.forgejo/workflows/pre-commit-check.yaml

  verify-chart:
    name: Verify chart version/changelog
    runs-on: codeberg-tiny-lazy
    if: contains(needs.pr-changes.outputs.changed-files, 'chart/')
    needs:
      - pr-changes
    steps:
      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify chart version
        uses: https://github.com/bjw-s/helm-charts-actions/verify-chart-version@2025.2.0
        with:
          chart: chart

      - name: Verify chart changelog
        uses: https://github.com/bjw-s/helm-charts-actions/verify-chart-changelog@2025.2.0
        if: ${{ !startsWith(github.head_ref, 'renovate/') }}
        with:
          chart: chart

  charts-lint:
    uses: ./.forgejo/workflows/charts-lint.yaml
    if: contains(needs.pr-changes.outputs.changed-files, 'chart/')
    needs:
      - pr-changes
    with:
      checkoutCommit: ${{ github.sha }}
      chartsToLint: '["chart"]'

  charts-test:
    uses: ./.forgejo/workflows/charts-test.yaml
    if: contains(needs.pr-changes.outputs.changed-files, 'chart/')
    needs:
      - pr-changes
    with:
      checkoutCommit: ${{ github.sha }}
      chartsToTest: '["chart"]'
